name: provider
description: Generate GetX provider files for dependency injection

version: 0.1.0+1


environment:
  mason: ^0.1.1


vars:
  name:
    type: string
    description: Provider class name
    prompt: What is the provider name?
