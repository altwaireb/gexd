name: getx
description: GetX project structure.

# The following defines the brick repository url.
# Uncomment and update the following line before publishing the brick.
# repository: https://github.com/my_org/my_repo

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.1.0+1

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ^0.1.1

# Variables specify dynamic values that your brick depends on.
# Zero or more variables can be specified for a given brick.
# Each variable has:
#  * a type (string, number, boolean, enum, array, or list)
#  * an optional short description
#  * an optional default value
#  * an optional list of default values (array only)
#  * an optional prompt phrase used when asking for the variable
#  * a list of values (enums only)
#  * an optional separator (list only)
vars:
  project_name:
    type: string
    description: Name of the project
    prompt: What is your project name?
  template:
    type: string
    description: Template type
    default: "getx"
  description:
    type: string
    description: Project description
    default: "A new Flutter project with GetX"
  organization:
    type: string
    description: Organization name
    default: "com.example"
  platforms:
    type: list
    description: Target platforms
    default: ["android", "ios"]
  is_getx:
    type: boolean
    description: Whether to use GetX architecture
    default: true
  is_clean:
    type: boolean
    description: Whether to use clean architecture
    default: false
  is_create:
    type: boolean
    description: Whether to create a new project
    default: true
  version:
    type: string
    description: Version of the project
    default: "0.1.0"
